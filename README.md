# Product-Category-Classification
This project builds a machine learning model to classify product categories using product names and descriptions.   It uses TF-IDF for feature extraction and Naive Bayes for classification, achieving ~82% accuracy.   The model also provides Top-3 category predictions with confidence scores for better insights.
